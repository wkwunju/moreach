# Claude Code Rules

## Git Operations

- **Do NOT automatically run `git push`** unless the user explicitly requests it.
- Commits can be made when appropriate, but pushing to remote requires explicit user confirmation.
