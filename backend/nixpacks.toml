[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "sh -c 'python scripts/migrate.py && export USE_ALEMBIC=true && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
