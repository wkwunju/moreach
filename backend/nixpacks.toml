[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
# Run migrations then start app - inline for reliability
cmd = "USE_ALEMBIC=true python scripts/migrate.py && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
